---
title: "4.1 Линия алюминия (подготовка материалов)"
sidebar: science
execute:
  echo: false
---

```{ojs}
//| output: false

// Загружаем данные из JSON
dataScript = FileAttachment("../data/db/data.json").json()

// Материалы доступные на Меркурии
mercuryMaterials = new Set(
  dataScript.planetMaterials
    .filter(pm => pm.planet_id === "mercury")
    .map(pm => pm.material_id)
)

// Словари для быстрого поиска
materialsById = new Map(dataScript.materials.map(m => [m.id, m]))
unitsById = new Map(dataScript.units.map(u => [u.id, u]))
planetsById = new Map(dataScript.planets.map(p => [p.id, p]))

// Функция для отображения виджета единицы
function unitWidget(unitId) {
  const unit = unitsById.get(unitId);
  if (!unit) return html`<div class="db-widget">Единица ${unitId} не найдена</div>`;

  const planet = planetsById.get(unit.production_planet_id);

  // BOM для этой единицы
  const bom = dataScript.unitMaterials
    .filter(um => um.unit_id === unitId)
    .map(um => {
      const material = materialsById.get(um.material_id);
      const isLocal = mercuryMaterials.has(um.material_id);
      const massKg = unit.mass_kg ? (unit.mass_kg * um.fraction_pct / 100).toFixed(2) : null;
      return { ...um, material, isLocal, massKg };
    });

  // Расчёт итогов
  const localPct = bom
    .filter(b => b.isLocal)
    .reduce((sum, b) => sum + b.fraction_pct, 0);
  const importPct = bom
    .filter(b => !b.isLocal)
    .reduce((sum, b) => sum + b.fraction_pct, 0);

  const bomRows = bom.map(b => html`
    <tr>
      <td>${b.material?.symbol ? html`<strong>${b.material.symbol}</strong> ` : ''}${b.material?.name || b.material_id}</td>
      <td>${b.fraction_pct}%</td>
      <td>${b.massKg || '—'}</td>
      <td class="${b.isLocal ? 'local' : 'import'}">${b.isLocal ? '✓ местный' : '⚠ импорт'}</td>
    </tr>
  `);

  return html`
    <div class="db-widget">
      <h4>${unit.name} <span style="color: #6c757d; font-weight: normal;">(${unit.id})</span></h4>
      <div class="meta">
        ${unit.mass_kg ? html`<strong>${unit.mass_kg.toLocaleString()}</strong> кг` : ''}
        ${unit.power_kw ? html` | <strong>${unit.power_kw.toLocaleString()}</strong> кВт` : ''}
        ${planet ? html` | Производство: ${planet.name}` : ''}
      </div>
      ${bom.length > 0 ? html`
        <table>
          <thead>
            <tr><th>Материал</th><th>%</th><th>кг</th><th>Источник</th></tr>
          </thead>
          <tbody>${bomRows}</tbody>
        </table>
        <div class="summary">
          Итого: <span class="local-pct">${localPct.toFixed(0)}% местное</span> | <span class="import-pct">${importPct.toFixed(0)}% импорт</span>
        </div>
      ` : ''}
      <div class="link">
        <a href="../data/nomenclature.html?unit=${unitId}">Подробнее →</a>
      </div>
    </div>
  `;
}
```

## Обзор

| Параметр | Значение |
|----------|----------|
| Вход | Al расплав 42 т/день из [дистилляции](line-distillation.qmd) |
| Выход | Полуфабрикаты: фольга, листы, проволока |
| Производительность | ~42 т Al/день |
| Энергопотребление | МНЛЗ 100 кВт + прокат ~100 кВт |

← [Назад к обзору](../factory-interior.qmd)

---

## Поток материалов

```{mermaid}
flowchart TD
    subgraph INPUT["ВХОД"]
        DIST["Дистилляция<br/>line-distillation"]
        DIST -->|"Al 42т/день<br/>1500°C"| MHD["МГД-ввод"]
    end

    subgraph CASTING["ЛИТЕЙНАЯ ЗОНА"]
        MHD --> TUNDISH["Промковш<br/>EQU-023"]
        TUNDISH --> MNLZ["МНЛЗ-Al<br/>EQU-006"]
        MNLZ --> BILLETS["Слитки 100×100 мм"]
    end

    subgraph ROLLING["ПРОКАТНАЯ ЗОНА"]
        BILLETS --> SPLIT["Разделение потока"]
        SPLIT -->|"60%"| FOIL["Фольгопрокат<br/>EQU-026<br/>50 мкм"]
        SPLIT -->|"30%"| SHEET["Листовой прокат<br/>EQU-008<br/>1-3 мм"]
        SPLIT -->|"10%"| WIRE["Волочение<br/>Ø1.6 мм"]
    end

    subgraph OUTPUT["ПОЛУФАБРИКАТЫ"]
        FOIL --> FOIL_OUT["Фольга 50 мкм<br/>~25 т/день"]
        SHEET --> SHEET_OUT["Листы 1-3 мм<br/>~13 т/день"]
        WIRE --> WIRE_OUT["Проволока Ø1.6<br/>~4 т/день"]
    end

    style INPUT fill:#f0e68c
    style CASTING fill:#fff3cd
    style ROLLING fill:#d4edda
    style OUTPUT fill:#cce5ff
```

---

## Детальное описание

### МГД-ввод расплава

Транспорт расплава алюминия 1500°C из линии дистилляции в купол фабрики через магнитогидродинамический канал. МГД-канал проходит сквозь стену купола, обеспечивая герметичность и минимизацию теплопотерь.

### Промковш (тандиш)

Буферная ёмкость 2-3 т для разделения потоков Al и Fe. Промковш оснащён двумя стопорами (заслонками), которые управляют подачей расплава в соответствующие МНЛЗ.

```{ojs}
unitWidget("EQU-023")
```

### МНЛЗ-Al (Машина непрерывного литья алюминия)

Непрерывное литьё слитков 100×100 мм с использованием медного кристаллизатора. Кристаллизатор охлаждается водой (замкнутый цикл с радиаторами).

```{ojs}
unitWidget("EQU-006")
```

### Три направления проката

Слитки алюминия распределяются на три линии проката в зависимости от назначения:

#### Фольгопрокат (60% потока, ~25 т/день)

Фольга толщиной 50 мкм для:
- [Сборки зеркал](factory-assembly-mirrors.qmd) (с TiO₂ покрытием)
- Газонепроницаемых мембран для куполов

```{ojs}
unitWidget("EQU-026")
```

#### Листовой прокат (30% потока, ~13 т/день)

Листы толщиной 1-3 мм для:
- [Сборки роботов](factory-assembly-robots.qmd) (корпуса, радиаторы)
- Вспомогательного оборудования

```{ojs}
unitWidget("EQU-008")
```

#### Волочение проволоки (10% потока, ~4 т/день)

Проволока Ø1.6 мм для WAAM 3D-печати корпусов и конструктивных элементов.

---

## Материальный баланс полуфабрикатов

| Полуфабрикат | т/день | % | Назначение |
|--------------|--------|---|------------|
| Фольга 50 мкм | ~25 | 60% | [Сборка зеркал](factory-assembly-mirrors.qmd), мембраны куполов |
| Листы 1-3 мм | ~13 | 30% | [Сборка роботов](factory-assembly-robots.qmd), радиаторы |
| Проволока Ø1.6 мм | ~4 | 10% | WAAM 3D-печать корпусов |
| **Итого** | **42** | **100%** | |

---

## См. также

- [Дистилляция шлака](line-distillation.qmd) - источник алюминия
- [Сборка зеркал](factory-assembly-mirrors.qmd) - использует фольгу
- [Сборка роботов](factory-assembly-robots.qmd) - использует листы и проволоку
- [Линия титана (PVD)](factory-line-titanium.qmd) - напыление на фольгу
